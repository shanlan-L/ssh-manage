{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the ssh-manage main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
